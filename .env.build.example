# Canonical build-arg templates: see nornir-docker/example._shared.build.env
# Copy to .env.build (and/or per-image .env.build.<tag-with-hyphens>) in your invocation directory,
# or use files under $NORNIR_DOCKER_USER_ROOT mirroring dev/prod paths — see docker-build.ps1 header.
