# Headless Nornir Docker dependencies.
# Scientific stack uses minimum versions (aligned with nornir-pyre/requirements-qt.txt) so pip can
# choose wheels for the active Python (e.g. 3.14) when exact pins lack cp314 wheels yet.
# Pure-Python / stable pins below keep installs predictable.

numpy>=2.2.5
scipy>=1.15.3
matplotlib>=3.10.3
pillow>=11.2.1
scikit-image>=0.25.2
imageio>=2.37.0
networkx>=3.4.2
tifffile>=2025.5.10
contourpy>=1.3.2
cycler>=0.12.1
fonttools>=4.58.0
kiwisolver>=1.4.8
lazy_loader>=0.4
pyparsing>=3.2.3
python-dateutil>=2.9.0.post0
packaging>=25.0

annotated-types==0.7.0
attrs==25.3.0
hypothesis==6.131.16
paho-mqtt==2.1.0
# Require 2.12+ so pip uses pydantic-core wheels with cp314 (avoid Rust backtrack to 2.11.x).
pydantic>=2.12.0
python-dotenv==1.1.0
six==1.17.0
sortedcontainers==2.4.0
typing-inspection>=0.4.0
# pydantic 2.12+ needs newer typing_extensions; keep loose so cp314 wheels resolve.
typing_extensions>=4.14.0
validators==0.35.0

# PyOpenGL: required for nornir-pyre import chain (gl_engine) during umbrella pytest collection.
PyOpenGL>=3.1.0
